from id_extractor import IDreader

def reference_graph(self, reference_id_list, comparable_id_list, sessionId,):
        self.reference_id_list = reference_id_list
        self.comparable_id_list = comparable_id_list
        self.sessionId = sessionId
